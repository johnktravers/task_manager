<h1>All Tasks</h1>

<% @tasks.each do |task| %>
  <div class="well">
    <h3><a href="/tasks/<%= task.id %>"><%= task.title %></a></h3>
    <p><%= task.description %></p>
    <%= link_to "Read More", task_id_path(task.id), class: 'btn btn-default'%>
  </div>
<% end %>
